spring.application.name=vhms

# MongoDB Atlas Connection
spring.mongodb.uri=mongodb+srv://natasharuth255_db_user:e0JVGUyszyzkiPny@ruth.tmhfj1q.mongodb.net/VHMS?retryWrites=true&w=majority&appName=Ruth
spring.mongodb.database=VHMS

# Server
# Allow overriding the port via the PORT environment variable; default to 8081 to avoid conflicts with existing services
server.port=${PORT:8081}
